import 'package:flutter/material.dart';

class Savedtab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Stack(
        children: [
          Center(child: Text("Saved Items will show up here..."),),
          
        ],
      ),
      
    );
  }
}